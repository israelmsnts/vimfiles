atalhos:
  surround
    S + caractere
  easeymotion
    \w + sequencia
  emmet
    ctrl + y +,
  multiple cursors
    ctrl [nxpESC]
  align
    v<enter>**|

instalar 
  mkdir -p ~/.vim/autoload ~/.vim/bundle && \
  curl -LSso ~/.vim/autoload/pathogen.vim https://tpo.pe/pathogen.vim
